python 2.7.X
postgres 9.3
postgis 2.1.X
django 1.7.X


Install pip (link to it)
Install homebrew

brew install python
brew install postgres
brew install postgis

pip install -r requirements.txt 


cd to mapusaurus directory

There needs to be a database setup.. at least to work with django

Requires Elasticsearch. Needs to be included as a dependency.


